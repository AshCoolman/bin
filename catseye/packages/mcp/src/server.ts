// MCP server placeholder — implemented in Phase 5 (T039–T048)
// Uses @catlens/core exclusively; no CLI subprocess.

export function startServer(_root: string): void {
  throw new Error('MCP server not yet implemented (Phase 5)')
}
